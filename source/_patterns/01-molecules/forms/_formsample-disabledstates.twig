{% from 'form.twig' import formgroup, formbutton %}

{{ formgroup(
    {
        "label": "Disabled input", 
    },
    [{
        "type":"input", 
        "disabled": true,
        "parameter":{
            "placeholder": "Disabled input",
        }
    }]
) }}
